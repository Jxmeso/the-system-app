rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /evidence/{taskId}/{fileName} {
      allow read: if true;
      allow create, update: if request.resource.size < 250 * 1024 * 1024;
      allow delete: if false;
    }
  }
}
